<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>로그인</h1>
    <!-- action : 요청을 보낼 주소 (제 서버를 보낸다는 가정하에 도메인을 빼버린 나머지 주소 입력하기)-->

    <!-- ex) localhost:8000/get/test 이라는 곳으로 보내고 싶다면 action = "/get/test" -->
    <!-- method : 어떤 방식의 요청을 사용할 지 (get or post)-->   

    <!-- <form action="요청을 보낼 주소" method="get"> -->
    <form action="/get" method="get" target="_blank" name="login"> 
        <!-- name에 들어가는 값이 백엔드에서 키값으로 쓰인다구~ -->
        <fieldset>
        <legend>로그인 정보</legend>
        <input type="text" name="id" />
        <input type="password" name="pw" />
        </fieldset>
        <br />
        <input type="radio" name="gender" value="top" id="top" /> <label for="top">남자</label>
        <input type="radio" name="gender" value="bottom" id="bottom" /> <label for="bottom">여자</label>
        <button type="submit">제출</button>      
    </form>

    <script>
        //이건 html처럼 나온다.
        //console.log(document.forms["login"])

        // 이건 태그처럼 나온다. 속성값이 어떤것이 있는지 확인하기 편함.
        console.dir(document.forms["login"][0])
    </script>



    <h1>회원가입 get</h1>
    <form action="/get_sign" method="get">
        <input type="text" name="id" />
        <input type="password" name="pw" />
        <input type="text" name="name" />
        <input type="text" name="email" />
        <button type="submit">회원가입 제출</button>  
        <!-- button type submit인 경우, 버튼을 감싸고 있는 form 태그의 정보에 맞게 폼을 제출함. -->
    </form>

    <h1>회원가입 post</h1>
 
    <form action="/post/ver2" method="post">
        <input type="text" name="id" value="id" pattern="^([a-zA-Z0-9가-힣]){4,12}$" title="소문자, 대문자, 한글, 숫자로 조합된 4-12로 입력" required/>
        <input type="password" name="pw" oninput="vaildCheckPw(this)"/>
        <div id = "err-pw"></div>
        <!-- this = 해당 태그를 넘긴다 인자로 -->
        <input type="text" name="name" minlength="4" required/>
        <input type="number" min="5" max="10" required/>
        <input type="password" name="email" />
        <button type="submit">회원가입 제출</button>  
        <!-- button type submit인 경우, 버튼을 감싸고 있는 form 태그의 정보에 맞게 폼을 제출함. -->
    </form>

    <script>
        function vaildCheckPw(el) {
            const regexPw = /^([a-zA-Z0-9]){8,16}$/ 
            // 자바스크립트에서 정규식을 담을땐 슬래쉬 안에 담는다!
                // regexPw.test("검사하고 싶은 값") => 참거짓 리턴

            const value = el.value 
            // 엘리먼트-> 벨류값을 담는다.
            const error = document.getElementById("err-pw")
            if(regexPw.test(value)) {
                console.log("정규식 일치")
            } else {
                error.innerHTML = "소문자, 대문자, 한글, 숫자로 조합된 4-12로 입력"
                console.log("정규식 불일치")
            }
        }
    </script>

</body>
</html>